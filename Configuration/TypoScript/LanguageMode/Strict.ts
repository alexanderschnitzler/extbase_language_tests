# The system will report an error if the requested translation does
# not exist. Basically this means that all pages with gray background
# in the Web > Info / Localization overview module will fail (they
# would otherwise fall back to default language in one way or another).
config.sys_language_mode = strict
