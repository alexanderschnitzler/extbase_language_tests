# The system will stay with the selected language even if the page
# is not translated and there's no content available in this language,
# so you can handle that situation on your own then.
config.sys_language_mode = ignore
